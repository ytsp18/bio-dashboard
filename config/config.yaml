# Bio Dashboard Configuration
#
# NOTE: This file is for REFERENCE ONLY.
# - User credentials are now stored in the DATABASE (not here)
# - Cookie key is stored in Streamlit secrets (not here)
# - This file is safe to commit to git
#
# Roles:
#   - admin: Full access including Admin Panel
#   - user: All features + upload, but no Admin Panel
#   - viewer: View only, cannot upload

# Settings (reference only - actual settings in database)
settings:
  allow_registration: true
  require_approval: true
  default_role: viewer

# Default users (for first-time migration to database only)
# These are migrated to database on first run, then database is used
# DO NOT put real passwords here - only hashed versions for migration
credentials:
  usernames:
    admin:
      email: admin@bio-dashboard.local
      name: Administrator
      password: $2b$12$ns/BFECtN89fYf1Rnk5MFOBhqSBls.Ht8yPUssmtEltdUJeVg0CyG
      role: admin
    operator:
      email: operator@bio-dashboard.local
      name: Operator User
      password: $2b$12$alzYTZXstdY2eGN/z6d3ZuH2BoN2gjvBuaVmD3cWTmqOx/0xIoFVS
      role: user
